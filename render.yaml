services:
  - type: web
    name: ferry-booking-backend
    env: php
    rootDir: .
    buildCommand: composer install --no-interaction --prefer-dist --optimize-autoloader
    startCommand: php artisan migrate --force && php artisan serve --host 0.0.0.0 --port $PORT
    plan: free
